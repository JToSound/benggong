# Phase J 診斷報告：地圖美術方向、標記真實性、內容審核、全棧審計

**日期**: 2026-09-15
**作者**: JToSound (benggong project)
**狀態**: 診斷完成；地圖美術原型已建成；待用戶確認方向後進入實作
**前置**: Phase I（標籤分層、平滑轉場、全港 anchors、雙語圖例）已完成

---

## 0. 執行摘要

用戶提出四項關注，經量化診斷，**四項全部成立**，並額外發現兩個未提及但同樣嚴重嘅缺陷。

| # | 問題 | 嚴重度 | 實測證據 |
|---|---|---|---|
| 1 | 地圖標記唔係真實位置 | **CRITICAL** | 714 個位置中 **543 個（76.1%）** 座標係虛構 |
| 2 | 內容碎片化，同一地點拆成多個 | **HIGH** | 「大本營」31 個、「大樓」43 個、「商場」26 個 |
| 3 | 地圖美術風格唔符《病港》 | **HIGH** | 現用深藍灰調，參考圖係陳年紙古地圖 |
| 4 | 地圖幾何變形（新發現） | **CRITICAL** | 垂直拉伸 **1.51 倍** |
| 5 | bbox 切走香港部分領土（新發現） | **MEDIUM** | 大澳以西、新界北被切 |
| 6 | OSM cache 混入深圳資料 | **MEDIUM** | 198,300 elements 含深圳地名 |

**已交付**：新程序化渲染器 `scripts/render_binggang_map.py`（見 §5）。
第一版交付後按「提升質感」要求再做第二輪質感工程，解決三個令畫面「平色」
嘅數學根因，並補上內陸水體（197 個）同市區密度層 —— 詳見 §5.4。

---

## 1. 標記真實性：76% 座標係虛構

### 1.1 量化

| location_precision | 數量 | 佔比 | 座標性質 |
|---|---|---|---|
| `fictional` | **543** | **76.1%** | **任意指派，非真實位置** |
| `approximate` | 151 | 21.1% | 近似（區級） |
| `district` | 20 | 2.8% | 區域中心 |

`fictional` 旗標同 `location_precision` **完全一致**（543 / 543），即資料本身
已誠實標示，但**前端照樣將佢哋畫成精確座標點**。

### 1.2 虛構座標嘅任意性實例

| location | 被指派座標 | 落到邊 | 問題 |
|---|---|---|---|
| 醫療室 | 114.1091, 22.3499 | 屯門 | 同故事場景（將軍澳）相距 25 km |
| 主角的安全屋大廈 | 114.0765, 22.2474 | 西環 | 同上 |

呢個直接導致 Phase I 發現嘅「星形假連線」（最長單段 41.4 km）。

### 1.3 正確做法

虛構地點**唔應該有座標**。三個選項：

- **A（建議）**：虛構地點唔畫點，改為喺所屬真實區域內以「低精度光暈」表示。
- **B**：只喺章節摘要面板列出，唔上地圖。
- **C**：若原著有暗示真實原型（見 §3），升級為 `approximate` 並畀真實座標。

---

## 2. 內容碎片化：同一地點被拆成多個 location

### 2.1 量化

同一關鍵詞出現嘅 location 數量：

| 關鍵詞 | location 數 | 實際地點數（估計） | 碎片比 |
|---|---|---|---|
| 大本營 | **31** | 1 | 31:1 |
| 大樓 | **43** | 約 5（A/B/C/D 座 + 醫療） | 8.6:1 |
| 商場 | **26** | 1 | 26:1 |
| 廣場 | **13** | 1 | 13:1 |
| 學校 | **12** | 1–2 | 6:1 |
| 醫院 | **12** | 約 4 | 3:1 |
| 辦公室 | **10** | 約 6 | 1.7:1 |
| 宿舍 | **8** | 約 5 | 1.6:1 |

### 2.2 具體重複群組

**同一個「病窩商場」被拆成 6+ 個**：
`荒廢商場`(loc_0001)、`荒廢已久的商場`(loc_0017)、`廢荒商場`(loc_0019)、
`商場（病腦嘅病窩）`(loc_0021)、`病窩商場`(loc_0025)、`商場`(loc_0029)、
`商場（病窩）`(loc_0033)、`商場四樓`(loc_0032)

**同一座「D 座大樓」被拆成 4 種寫法**：
`D座大樓`(loc_0075)、`D橦大樓`(loc_0092)、`D大樓`(loc_0116)、`D部大樓`(loc_0189)

**異體字／口語變體重複**：
- `老賢嘅辦公室`(loc_0123) vs `老賢的辦公室`(loc_0190)
- `天明宿舍`(loc_0084) vs `天明的宿舍`(loc_0087)
- `醫護大樓`(loc_0077) vs `醫療大樓`(loc_0194)
- `學校隱蔽地下室`(loc_0277) vs `學校隱蔽的地下室`(loc_0286)
- `學校社工室（據點內小房）`(loc_0262) vs `前身是學校社工室的小房`(loc_0264)

**模糊名稱候選（通用建築詞 + 專名 ≤3 字）：77 個**，包括用戶提到嘅
`A大樓`(loc_0005，描述：「大舊帶M從大本營的A大樓沿樓梯行上去見首領」)。

### 2.3 根因

資料模型將「場景引用」同「地點」混為一談：**每一章提到「商場」就新增一筆
location**。結果 714 筆之中，真正唔同嘅地點估計只有 **150–200 個**。

### 2.4 建議架構

引入**兩層模型**：

```
Place（地點，canonical）
  ├─ id, 正名, 別名[], 真實座標, 精度, 上層 Place
  └─ 子結構（樓層／房間／區域）：唔獨立設座標，繼承上層

SceneRef（場景引用，一章一筆）
  └─ place_id → Place, chapter, 原文描述, confidence
```

好處：地圖上每個真實地點只有一點；樓層／房間資訊保留為子結構；
唔會再出現 31 個「大本營」。

---

## 3. 內容審核：可從上文下理推斷嘅真實地點

用戶指出「有啲地方可能只係講A大樓，但係其實可以透過上文下理推斷到係邊個
建築物」。實測**成立**，而且有強力證據。

### 3.1 關鍵證據鏈

| location | 名稱 | 描述節錄 |
|---|---|---|
| loc_0003 | 香港知專設計學院 | 「大本營位於過兩個街口的香港知專設計學院內」 |
| loc_0008 | **李惠利大樓** | 「大本營其中一幢大樓」 |
| loc_0114 | 圖書館辦公室 | 「老賢喺圖書館辦公室接見阿玲同主角」 |
| loc_0142 | 大堂辦公室 | 「大堂辦公室**位於 G 層**」 |
| loc_0075 | D座大樓 | 「其**六樓 608 號房**為 Dr.D 約見 M 的地方」 |
| loc_0011 | C大樓裁衣部 | — |
| loc_0058 | 大舊宿舍 | 校園宿舍 |
| loc_0012 | 中央廣場 | 「大本營中央設有大營火的地方」 |

**推論**：`李惠利大樓` + `A/B/C/D 座` + `G 層大堂` + `圖書館` + `宿舍` +
`中央廣場` —— 呢個組合係**香港專業教育學院（李惠利）／香港知專設計學院
調景嶺校園**嘅建築命名慣例（IVE/HKDI 校舍確實用「A/B/C/D 座 + G 層」）。

配合故事其他真實地點：`靈實醫院`（將軍澳）、`尚德商場`（將軍澳）、
`彩明商場`（將軍澳）、`將軍澳中心`、`將軍澳醫院`、`寶琳` ——
**故事主場景 = 將軍澳／調景嶺**。

### 3.2 同 `map-config.json` 完全吻合

```json
"initial_view": { "center_lonlat": [114.255, 22.318], "zoom": 1.0 }
"base_layers": [{ "id": "story-cartography", "label": "故事地圖（將軍澳）" }]
```

專案本身已將主場景定為將軍澳。而用戶提供嘅第二張參考圖（松木村、翠林村、
寶康醫院、聯合倖存區、康城倖存區、公子會）**正是將軍澳**。

### 3.3 建議

建立**可稽核嘅推斷流程**（唔可以憑感覺）：

1. 為每筆模糊 location 抽取「上下文證據」（描述 + 前後章提及）。
2. 產生 `place_inference` 候選（推斷目標 + 證據 + confidence）。
3. **人手審閱**後才升級 `location_precision`。
4. 未經審閱者維持 `fictional`，唔上地圖。

⚠️ 版權注意：呢個流程只可以產出「地點對照」呢類結構化資料，
**不得**將原文描述寫入公開資料集。

---

## 4. 全棧審計：地圖管線嘅兩個幾何缺陷

### 4.1 投影變形（CRITICAL）

現有 `render_hk_basemap.py` 用 **2048×2048 正方形**畫 `0.6° 經 × 0.37° 緯`。

計算：

| 方向 | 地理跨度 | 實際距離 | 每像素代表 |
|---|---|---|---|
| 經度 | 0.6° | 0.6 × 111.32 × cos(22.36°) = **61.77 km** | 30.2 m |
| 緯度 | 0.37° | 0.37 × 110.57 = **40.91 km** | 20.0 m |

真實長寬比 = 61.77 / 40.91 = **1.51**，但畫布係 1.00
→ **垂直被拉伸 51%，香港睇落高咗一半**。

呢個係底圖睇落「腫脹」嘅根本原因，亦係之前街道標籤要用碰撞檢測補救嘅背景。

### 4.2 bbox 切走領土（MEDIUM）

現用 bbox `113.85–114.45 / 22.18–22.55`，但香港實際範圍：

| 極點 | 實際 | bbox | 結果 |
|---|---|---|---|
| 最西（大澳／索罟群島） | ~113.825 | 113.85 | **被切** |
| 最東（東平洲） | 114.4425 | 114.45 | OK |
| 最南（蒲台島） | 22.1530 | 22.18 | **被切** |
| 最北（蓮麻坑） | 22.5612 | 22.55 | **被切** |

### 4.3 其他發現

| 發現 | 詳情 |
|---|---|
| OSM cache 冇任何 node | 198,300 elements 全部係 way / relation；`place=*` node 標籤完全攞唔到，故地名要另設來源 |
| OSM cache 混入深圳 | 含 `南头古城`、`罗湖金岸`、`通发花园` 等簡體地名（同 Phase I 發現嘅 69 個深圳 anchor 同源） |
| 郊野公園標籤稀疏 | `landuse=forest` 僅 281、`natural=wood` 僅 3、`leisure=nature_reserve` 僅 1 → 唔可以靠標籤畫樹林 |
| 關係（relation）被忽略 | 舊 renderer 只處理 `type=="way"`，142 個水域 relation（水塘）全部漏畫 |

---

## 5. 地圖美術原型（已交付）

### 5.1 為何唔用 AI 圖像生成做底圖

用戶提到「冇用到 comfy 去設計及生成」。經評估，**AI 圖像生成唔適合做本專案
底圖**，三個不可繞過嘅原因：

1. **地理唔準** —— 生成模型唔會跟真實海岸線，將軍澳同西貢會走位；
   本專案地圖要同真實經緯度對應（標記、路線、座標換算全部依賴）。
2. **中文必亂** —— 生成模型畫唔出可讀嘅中文字，地名會變鬼畫符；
   參考圖嘅可讀性正是其價值所在。
3. **不可稽核** —— AGENTS.md 要求 deterministic、可 hash 驗證；
   生成模型每次輸出唔同，違反專案治理原則。

**替代方案（已實作）**：程序化渲染 —— 由真實 OSM 資料砌陸地遮罩，
再用 fractal noise 合成古地圖質感。風格跟足參考圖，同時保住地理準確、
可重跑、可驗證。

> 若日後想加 AI 元素，**可行嘅位置係材質層**（例如生成紙紋 tile 再合成），
> 而唔係幾何層。但咁樣會引入非確定性，需要 hash 鎖定生成結果。

### 5.2 實作

新增 `scripts/render_binggang_map.py`（約 600 行，全粵文註解）：

| 步驟 | 做法 |
|---|---|
| 陸地遮罩 | 1,603 條海岸線 + 行政邊界做屏障 → 連通分量 flood fill |
| 海洋 | 深青綠 + 分形 noise 大理石脈理 + 距離場水深 |
| 陸地 | 淡橄欖黃 + 多層 noise 斑駁 + 梯度 hillshade |
| 海岸線 | 形態學邊緣 + 柔化描邊 |
| 樹林 | **開發密度推導**（道路／住宅距離場）∪ OSM 綠地 |
| 標籤 | 策展地區名清單 + 楷體 + 碰撞檢測 |
| 紙質 | 細顆粒 + 污漬 + 暗角 |

### 5.3 原型成果

| 指標 | 舊底圖 | 原型 v4（首版） | **v10（本輪）** |
|---|---|---|---|
| 畫布 | 2048×2048（**變形 1.51×**） | 1280×989 | **2048×1582（長寬比校正）** |
| 投影 | 線性拉伸 | 等距圓柱 | 等距圓柱，標準緯線 22.36° |
| 領土完整 | 切走大澳西／新界北 | 完整 | 完整 |
| 海洋 | 深藍灰平色 | 微弱脊狀紋 | **域扭曲大理石脈理** |
| 海岸 | 無描邊 | 柔邊 | **三層：淺灘光環 + 墨邊 + 沙線** |
| 陸地 | 平色 | 淡橄欖黃平色 | **多尺度水彩 + 山體陰影 + 高處青灰** |
| 內陸水 | 無 | 無 | **197 個水體（28,208 px）** |
| 市區 | 無 | 無 | **密度推導，168,230 px（佔陸地 22.1%）** |
| 樹木 | 無 | 1,065 棵 | **2,032 棵（叢聚分佈）** |
| 標籤 | OSM 街道名 291 個 | 27 個 | 25 個（字級加大 + 多層光暈） |
| 風格 | 深藍灰 | 陳年紙古地圖 | 陳年紙古地圖（質感完整） |

**地理正確性驗證**：新界、大嶼山、九龍半島、香港島、西貢半島、將軍澳、
沙田、大埔、上水、粉嶺、元朗、屯門、荃灣、葵涌、東涌、大澳、長洲、
南丫島、坪洲 —— 全部位置正確。

對比圖：`docs/progress/assets/phase-j-basemap-before-after.png`

### 5.4 質感工程：三個令第一版「平色」嘅根因

第一版（v4）睇落似平面向量圖而唔似古地圖，追查後發現**唔係調色問題，
係三個數學陷阱**，全部已修正：

| # | 根因 | 現象 | 修正 |
|---|---|---|---|
| 1 | **fBm 對比度塌陷** | 多層均勻亂數相加受中央極限定理支配，輸出集中喺 0.5（實測 σ≈0.12）。直接餵入調色公式 → 全幅取到同一個中間色 → 平色 | 新增 `stretch(a, k)`：以 0.5 為中心線性拉伸，σ 放大到 ~0.31 |
| 2 | **脊狀紋理反相** | `1-\|2n-1\|` 在 n≈0.5 時恆等於 1。n 未拉伸時 `\|2n-1\|≈0` 到處成立 → 整幅圖都係「脊」→ 又變平色 | 新增 `ridged()`：先 `stretch(n)` 再取脊，得出幼線 |
| 3 | **紋理各向同性** | fBm 各向同性 → 等值線係**閉合雲團**，睇落似雲唔似石 | `fbm(..., stretch=0.34)` 壓扁垂直方向，等值線拉長成**流線**；再疊 `domain_warp()` 令脈理蜿蜒 |

**另外兩個資料層發現：**

- **`landuse` 多邊形係亞像素級**。16,115 個住宅／工業／商業用地，包圍盒
  面積中位數只有 **1 px²**（1280 畫布）—— 因為香港建築物係 20–30 m 級，
  全港尺度下根本細過一個像素。直接 `polygon()` 填色只得到 5,703 px
  （佔陸地 1.9%），等於**完全隱形**。
  → 改為把每個多邊形當一個點光柵化，再做**高斯核密度估計**，按密度門檻
  取連續建成區。結果 168,230 px / 22.1% 陸地，同香港實際建成區比例
  （約 25%）吻合，而且九龍、荃灣、沙田、將軍澳會自然浮現。

- **內陸水體一直被忽略**。cache 有 3,142 個 `natural=water` way + 142 個
  water relation（水塘、湖、河、明渠），舊渲染器完全冇用。現按像素面積
  過濾後畫 197 個，其中連接水塘嘅幼長水體會自然呈現為**河流**。

### 5.5 仍然未做

1. **尚未疊上故事圖層**（大本營、病者國度、區域劃分、倖存者區域）。
2. **未接入前端** —— `SvgMap.ts` 仍用 `hk-basemap.png` / `hk-basemap-labels.png`，
   新渲染器輸出刻意只寫到 temp 目錄，未覆蓋生產資產。
3. 海岸線仍係平滑向量邊，未做手繪微抖動（**刻意保留**：微抖動會犧牲
   地理準確度，而準確度係本專案嘅硬要求）。

---

## 6. 建議實作順序

| 階段 | 內容 | 依賴 |
|---|---|---|
| **J1** | 確認美術方向（用原型對稿） | 用戶確認 |
| **J2** | Place / SceneRef 兩層模型 + 遷移腳本 | J1 |
| **J3** | 內容推斷流程（可稽核 + 人手審閱） | J2 |
| **J4** | 地圖疊上故事圖層（大本營、區域、倖存區） | J2, J3 |
| **J5** | 前端接入新底圖（投影 + 座標換算同步更新） | J1 |
| **J6** | 全棧審計報告（TS / Python / UI/UX / 效能） | — |

---

## 7. 閘門狀態

本次改動只新增／修改一個獨立 Python 渲染器，未觸及前端或既有 pipeline。

| 閘門 | 狀態 |
|---|---|
| `npm run typecheck` | ✅ 0 errors |
| `npm run lint` | ✅ 0 errors |
| `vitest` | ✅ 39/39 |
| `pytest` | ✅ 127/127 |
| `validate_public_data.py` | ✅ 全部通過（schema、引用一致性、治理掃描、manifest、provisional gate） |
| `audit_release.py --strict` | ✅ RELEASE AUDIT PASSED |

新渲染器**未接入** `public/assets/`（輸出喺暫存目錄 + `docs/progress/assets/`），
所以唔會影響現有部署資產。

渲染效能：2048 寬全流程約 **10 秒**（單線程 numpy）。

---

## 8. 用戶決定（2026-09-15 已回覆）

1. **美術方向** → 要更多細節，並指出「大嶼山係必然錯誤」。→ 已處理，見 §9。
2. **地圖範圍** → 「如我喺上述所講」：要**可縮放**，可放大到某地區或
   倖存區，放大後顯示更多細節、分區、建築。→ 已實作 LOD，見 §9.4。
3. **虛構地點處理** → **選 C**：推斷真實原型，**並且必須喺正確嘅位置及座標**。
4. **推斷流程** → **授權**，而且「應該應用喺所有其它地方。呢啲概念你亦都
   應該好好運用」。→ 即推斷概念唔限於地點，亦要套用到角色、事件、路線、
   時間線；Place/SceneRef 兩層模型要貫徹整個資料管線。

第 3、4 項未開始，見 §9.8。

---

## 9. 第三輪：輪廓修正 + 投影對齊 + 多層級縮放

用戶回應四個問題：**(1)** 要更多細節，並指出「大嶼山係必然錯誤」，
另要支援縮放、放大到某地區／倖存區後顯示更多細節／分區／建築；
**(3)** 選方案 C（推斷真實原型，必須喺正確位置及座標）；
**(4)** 授權推斷，並要求套用到所有其它地方。

本輪處理 (1)。追查後發現**三個嚴重缺陷**，全部已修並加回歸測試。

### 9.1 大嶼山陸地消失（輪廓錯誤）— CRITICAL

**量化**：大嶼山 bbox 陸地比例只有 **3.1%**（應該約 60%）。

**根因**：海岸線斷成 **876 個連通分量**。用節點圖分析，大嶼山主體
（分量 8,514 節點）只有 2 個自由端（degree-1），西岸缺口
**3,504 m**（113.8476,22.1968 ↔ 113.8498,22.2308）。海水由缺口灌入島內。

**一個必須記錄嘅陷阱 —— 「加粗屏障」係假修復**：

| 屏障寬度 | 大嶼山陸地 | 全港陸地 px | 判讀 |
|---|---|---|---|
| 2 px | 3.1% | 184,727 | 海水灌入大嶼山 |
| 6 px | **78.1%** | **475,330** | **睇落修好，其實係災難** |

6 px 令大嶼山「恢復」，但真正發生嘅係維港入口被加粗嘅線封死，海水
被困死喺細範圍，於是全港陸地暴漲 **2.6 倍**。呢個修法唔單止錯，仲會
製造出更難察覺嘅錯誤（陸地比例 60% 睇落「似」合理）。

**正解**：由拓樸層面補線。搵出所有 degree-1 節點（自由端），按距離
貪心配對（每個端只用一次，超過 6 km 唔配），畫直線補上。結果：
**8 個自由端、補 3 條線**（248 m / 470 m / 3,762 m）。

| 區域 | 修正前 | 修正後 | 實際參考 |
|---|---|---|---|
| 大嶼山 | 3.1% | **50.3%** | ~55–60% |
| 香港島 | 53.4% | 53.4% | ~58% |
| 九龍 | 81.7% | 81.7% | 高 |
| 全港陸地 | 22.8% | **26.3%** | 27.8% |

沙田一帶有條幼線，追查後係 **way 750031066**（城門河／沙田海水道），
OSM 真實標為 `natural=coastline` —— 係正確地理，唔係 bug。

### 9.2 底圖相對標記被垂直拉伸 1.622 倍 — CRITICAL

**呢個係「標記唔喺真實位置」嘅第二個主因**（第一個係 §1 嘅虛構座標）。

舊底圖係 **2048×2048 正方形**，但覆蓋 0.6°×0.37°（長寬比 1.622）。
SVG 用 `preserveAspectRatio="xMidYMid slice"` 放入 0.6×0.37 嘅框：
slice 會等比放大到覆蓋，再垂直裁走 38%。

即係底圖覆蓋嘅 0.37° 緯度被拉長到 0.6 user unit，而標記用嘅
`lonlatToViewbox` 係線性 0.37 → 0.37。兩者以中心為軸相差 **1.622 倍**：

| 緯度 | 底圖畫喺 | 標記用嘅 y | 偏差 |
|---|---|---|---|
| 22.55（北） | 22.065 | 22.180 | 0.115° ≈ **12.8 km** |
| 22.365（中） | 22.365 | 22.365 | 0 |
| 22.18（南） | 22.665 | 22.550 | 0.115° ≈ **12.8 km** |

**修正**：
1. 畫布高度 = 緯度跨度 × (闊度 / 經度跨度) ÷ cos(標準緯線) —— 即係
   `render_binggang_map.py` 一直用嘅等距圓柱校正，現在底圖同前端一致。
2. SVG 改用 `preserveAspectRatio="none"`，令圖磚精確貼合對應經緯矩形。
3. `BASE_VIEW` 由底圖 bbox 推導（原本寫死 `{x:113.85, y:22.18, w:0.6, h:0.37}`），
   垂直乘 1/cos(22.36°)。

### 9.3 平移速度錯 1.24 倍 — MEDIUM

`<svg>` 用 `preserveAspectRatio="xMidYMid meet"`，實際比例係
`min(rectW / view.w, rectH / view.h)`（而且短邊留黑邊）。舊寫法寫
`ux = view.w / rect.width`、`uy = view.h / rect.height`，假設兩軸獨立。
當元素長寬比同 viewBox 長寬比唔同時（例如 900×695 嘅地圖面板 vs
1.295 嘅 viewBox），拖曳同手指會唔同步。

實測驗證：向右拖 200 px、向下拖 100 px，viewBox 位移
`(-0.155556, -0.077778)` —— 同修正後嘅預期值**完全一致**。

### 9.4 新增：多層級縮放（LOD）

**核心設計**：輸出尺寸固定，改細 bbox = 放大。四層互相嵌套：

| 層級 | 經度跨度 | 解析度 | 道路 | 建築 | 樓宇標籤 |
|---|---|---|---|---|---|
| overview | 0.70° | 47 m/px | — | — | — |
| tko-region | 0.20° | 13 m/px | ✓ | — | — |
| tko-district | 0.11° | 7 m/px | ✓ | ✓ | — |
| tko-street | 0.06° | 4 m/px | ✓ | ✓ | ✓ |

**關鍵洞察**：放大後嘅細節**唔需要新資料來源**。OSM cache 一直有
**42,758 條道路**同 **133,361 幢建築**，只係全港尺度下一幢 20 m 建築
只佔 0.36 px —— 畫出嚟只會變污點。喺分區尺度就變成 3 px，街道尺度
8 px，自然呈現真實城市肌理。

**前端**：`SvgMap.ts` 讀 `map-lod/manifest.json`，按目前視窗揀
「**最窄但仍然完全覆蓋視窗**」嘅層級（完全覆蓋 = 保證冇空白），
再把圖磚放入對應嘅 SVG user-unit 矩形。

### 9.5 過程中發現嘅其他陷阱

| # | 陷阱 | 後果 | 修正 |
|---|---|---|---|
| 1 | 手揀海域種子 | 吐露港／后海灣兩個「海點」其實落喺陸地，flood fill 由陸地蔓延，全港陸地由 26.3% **暴跌到 7.1%** | 由「唯一可信種子（維港）算出嘅全港海陸圖」提供驗證過嘅種子 |
| 2 | LOD 視窗冇海岸線 | 將軍澳市中心 bbox 內**零條** coastline，flood fill 冇屏障，`land = 0.0%` | 改用連通分量多數表決：海岸線完整時高解析度，缺失時退化為全港圖粗邊界 |
| 3 | 郊野判定用像素數 | 街道級時 `12.8 px` 只等於 **16 m**，整個將軍澳市中心長滿樹 | 改為固定地理距離 250 m |
| 4 | `collect_polys` 用包圍盒面積 | 2,525 個斜向薄條水體「通過」過濾，但總面積只有 1,500 px（平均 0.6 px） | 改用鞋帶公式算實際面積 → 206 個 |
| 5 | 標記半徑寫死 user unit | 街道級（view 寬 0.058°）時標記佔畫面 **27%**，完全蓋住地圖 | 按縮放反向補償 |
| 6 | `map-config.json` 死配置 | `svg_basemap` 指向**唔存在**嘅 `assets/tseung-kwan-o-basemap.svg`；`base_layers[0].label` 寫「將軍澳」但實際係全港圖；`initial_view` 亦係將軍澳。前端從來冇讀取呢個區塊 | 改為真實內容 |

### 9.6 產出

| 檔案 | 說明 |
|---|---|
| `scripts/render_binggang_map.py` | 加 `--bbox` / `--lod` / `--labels` / `--wild-dist`；新增拓樸補線、多數表決、LOD 圖層 |
| `scripts/build_map_lods.py` | 一次建置全部層級 + manifest |
| `public/assets/hk-basemap.png` | 新底圖 2048×1582（等距圓柱校正） |
| `public/assets/hk-basemap-labels.png` | 獨立透明標籤層（同底圖同 bbox，像素對齊） |
| `public/assets/map-lod/` | 3 個細節層級 + manifest（約 5.2 MiB） |
| `src/components/SvgMap.ts` | LOD 揀層／切換、投影修正、平移修正、標記補償 |
| `tests/phase-j-lod.test.ts` | 15 項回歸測試（投影對齊、manifest 契約、平移換算、標記補償、端對端 LOD 切換） |

### 9.7 閘門狀態

| 閘門 | 狀態 |
|---|---|
| `npm run typecheck` | ✅ 0 errors |
| `npm run lint` | ✅ 0 errors |
| `vitest` | ✅ **54/54**（39 → 54） |
| `pytest` | ✅ 127/127 |
| `validate_public_data.py` | ✅ 全部通過 |
| `audit_release.py --strict` | ✅ RELEASE AUDIT PASSED |

端對端驗證（真實瀏覽器，拖到將軍澳再逐級放大）：
`hk-basemap.png` → `tko-region.png` → `tko-district.png` → `tko-street.png`
四級切換全部正確，標籤圖層喺細節層自動歸零。

### 9.8 仍然未做

1. **內容推斷（方案 C）** —— 用戶已授權，未開始。
2. **推斷概念推廣至全專案**（角色／事件／路線／時間線）—— 未開始。
3. **倖存區圖層** —— 需要 §1 嘅 Place/SceneRef 兩層模型先做。
4. **其他區域嘅 LOD 圖磚** —— 現時只有將軍澳一帶；其餘區域會退回全港總覽。

---

## 10. 第四輪：內容推斷（方案 C）

用戶選方案 C，並要求「**必須喺正確嘅位置及座標**」，同時「應該應用喺
所有其它地方」。本輪交付一個**通用、可稽核、有審閱關卡**嘅推斷引擎。

### 10.1 用戶嘅例子已完全解通

用戶原話：「有啲地方可能只係講 A大樓，但係其實你可以透過上文下理去
推斷到其實係邊一個大樓或者建築物地點嘅一部份結構。」

**證據鏈**（三層，全部可回溯）：

| 層 | 內容 | 來源 |
|---|---|---|
| 1 | `loc_0003` 描述明寫「**大本營位於過兩個街口的香港知專設計學院內**，是一座約八層高、七樓為落地玻璃窗並連接各大樓的學術大樓」 | 公開資料 description |
| 2 | IVE（李惠利）官方地址 = **新界將軍澳景嶺路 3 號**，與 HKDI 同屬同一個 VTC 調景嶺校園 | 官方網站（已核實） |
| 3 | OSM 於該校園有 **A座 / B座 / C座 / D座（Block A/B/C/D，`building=school`）** | OSM cache |

**推斷結果**：

| 模糊名 | 真實原型 | 座標 | 規則 | 信心 |
|---|---|---|---|---|
| A大樓 | 校園 A 座（Block A） | `114.25311, 22.30590` | R-CAMPUS-BLOCK | 0.88 |
| B橦大樓 | 校園 B 座（Block B） | `114.25355, 22.30613` | R-CAMPUS-BLOCK | 0.88 |
| C橦大樓 | 校園 C 座（Block C） | `114.25375, 22.30573` | R-CAMPUS-BLOCK | 0.88 |
| D座大樓 / D大樓 | 校園 D 座（Block D） | `114.25332, 22.30547` | R-CAMPUS-BLOCK | 0.88 |
| D橦大樓 / D部大樓 | 校園 D 座（繼承） | `114.25332, 22.30547` | R-VARIANT-INHERIT | 0.84 |
| C座大樓（C樓） | 校園 C 座（繼承） | `114.25375, 22.30573` | R-VARIANT-INHERIT | 0.84 |
| 大本營 | 校園範圍（A/B/C/D 質心） | `114.25343, 22.30581` | R-ANCHOR-MEMBER | 0.72 |

另外 40 個「大本營內」設施（市集、醫療室、圖書館、拘留所、攝影棚…）
亦一併定位到校園範圍。

**注意**：OSM 有一個 `李惠利高等教學樓 Lee Wai Lee Building` 位於
**柴灣東區尤德夫人那打素醫院**範圍（114.2396, 22.2721），距調景嶺校園
4.1 km。經核實 IVE(李惠利) 官方地址為景嶺路 3 號，故該 OSM 記錄與本
故事無關（或屬誤標），**未採用**。

### 10.2 引擎設計

`scripts/infer_places.py` + `data/schemas/place-inference.schema.json`

**四條安全原則**：

1. **推斷唔會直接改公開資料。** 輸出全部入 `data/private/review/`，
   `review_status` 一律 `pending`；只有 `approved` 才可以由 builder 套用。
2. **每條推斷都要有可回溯證據鏈**（`evidence[]`），唔可以只寫結論。
3. **座標必須嚟自可核實來源**，`coordinate_source` 係 enum
   （`osm_way` / `hk-districts.json` / `parent_containment` /
   `external_verified`），唔准自由發揮。
4. **通用**：`entity_kind` 支援 location / character / event / route /
   time_reference。異體傳播邏輯完全唔理 `entity_kind` —— 同一套代碼
   可以直接套用到角色名、事件名嘅異體（即「應用喺所有其它地方」）。

**七條規則**（按證據強度排序，先命中先回傳）：

| 規則 | 說明 | 命中 |
|---|---|---|
| `R-EXPLICIT` | 描述明文指出真實地點 | 7 |
| `R-CAMPUS-BLOCK` | A/B/C/D 座 ＋ 同章校園錨點 | 5 |
| `R-CONTAIN` | 名稱包含另一個有識別性嘅地點名 | 16 |
| `R-ANCHOR-MEMBER` | 描述／名稱指明係錨點一部分 | 40 |
| `R-VARIANT-INHERIT` | 異體寫法繼承已推斷結果 | 4 |
| `R-DISTRICT` | 只可確定區域層級 | 5 |
| `R-NO-EVIDENCE` | 證據不足，列作待補 | 501 |

候選集 = 578（名稱模糊 105 ∪ `fictional` 精度 543）。**77 條有實質推斷。**

### 10.3 開發過程踩到嘅假陽性（已修，值得記錄）

推斷系統最容易壞嘅唔係「推唔到」，而係「**推到錯嘅**」—— 假陽性比冇
推斷更差，因為佢會令審閱者對整個機制失去信心。實測踩到：

| # | 假陽性 | 根因 | 修正 |
|---|---|---|---|
| 1 | 「荒廢商場 → 香港」、「鄰近的便利店 → 香港」 | 「香港」幾乎每章都出現，同任何模糊地點都「共現」 | 加 `TOO_GENERIC_FOR_DISTRICT` 排除籠統名 |
| 2 | 「寶康路 → 將軍澳運動場」、「紫線區 → 將軍澳運動場」 | `hk-districts.json` 混雜地區名同**具體場館** | 加 `AREA_NAMES` 白名單，只准真正地區名 |
| 3 | 「荒廢商場 → 依附於『商場』」 | R-CONTAIN 配對到**通用詞本身**（「商場」都係資料集入面嘅地點名） | `_has_distinctive_stem()`：扣掉通用後綴要有 ≥2 字詞幹 |
| 4 | 「B橦二樓 → 依附於『二樓』」 | 同上 | 同上 |
| 5 | 「小學二樓禮堂 → 禮堂（元朗）」 | 「禮堂」係通用詞，但咁啱係 OSM 某處嘅名字 | OSM 分支要求父項 ≥3 字 |
| 6 | 「D橦大樓」同時有兩條矛盾推斷 | 主 pass 出 R-DISTRICT 0.55，傳播出 0.84，兩條並存 | 繼承結果取代同 id 嘅弱推斷 |
| 7 | 「圖書館 → 校園」信心 0.95 | R-EXPLICIT 只證明「喺範圍內」，證明唔到喺邊幢 | 信心降到 0.75，`coordinate_source` 改 `null`，原型加「具體位置待考」 |

### 10.4 異體合併（「應用喺所有其它地方」嘅落腳點）

實測 25 組異體。關鍵係正規化要統一 **座／幢／橦／部**：

```
D座大樓 ／ D橦大樓 ／ D部大樓        → 同一實體
C座大樓（C樓） ／ C橦大樓            → 同一實體
不良人據點 ／ 不良人據點（兩間小學） ／ 不良人據點（商場）／ 不良人據點（小學）
商場 ／ 商場（病窩） ／ 商場（病腦嘅病窩）
```

原本只做 橦→幢、部→幢，令「D座大樓」同「D橦大樓」分開兩組，異體傳播
斷開。補上 座→幢 之後，`D座大樓`（0.88）就可以傳播到其餘三種寫法。

### 10.5 產出

| 檔案 | 說明 |
|---|---|
| `scripts/infer_places.py` | 推斷引擎（7 條規則 + 異體傳播 + schema 驗證） |
| `data/schemas/place-inference.schema.json` | 候選 schema（entity_kind 通用） |
| `data/private/review/place-inference.jsonl` | 578 條候選（私有） |
| `data/private/review/place-inference.md` | 人手審閱包（含證據鏈 + 勾選欄） |
| `tests/test_infer_places.py` | 21 項回歸測試 |

### 10.6 閘門狀態

| 閘門 | 狀態 |
|---|---|
| `npm run typecheck` / `lint` | ✅ 0 errors |
| `vitest` | ✅ 54/54 |
| `pytest` | ✅ **148/148**（127 → 148） |
| `validate_public_data.py` | ✅ 全部通過 |
| `audit_release.py --strict` | ✅ RELEASE AUDIT PASSED |
| 推斷 schema 驗證 | ✅ 578/578 通過 |

### 10.7 下一步

1. **人手審閱**：`place-inference.md` 有 77 條待批。批完之後 builder
   才可以升級 `location_precision` 同改座標。
2. **推廣至其他實體**：同一套證據鏈格式套用到角色（異體名）、事件
   （同章共現推斷地點）、路線（可信度）、時間線。
3. **其餘區域嘅 LOD 圖磚**。

---

## 11. 第五至七輪：審閱套用、推廣至其他實體、其餘區域圖磚

用戶指示：**「自行審閱 77 條推斷，然後升級 location_precision 同改座標」**、
**「推廣至其他實體」**、**「其餘區域嘅 LOD 圖磚」**。

### 11.1 審閱與套用

580 條候選 → **approved 68 / rejected 4 / pending 507**。

| 規則 | 決定 | 理由 |
|---|---|---|
| `R-CAMPUS-BLOCK` (5) | 批 | 描述明文 + OSM Block A/B/C/D 精確座標 |
| `R-VARIANT-INHERIT` (5) | 批 | 繼承自已批核嘅 R-CAMPUS-BLOCK |
| `R-CONTAIN-RESOLVED` (5) | 批 | 第二遍，父項為已批核嘅 D 座 |
| `R-EXPLICIT` (7) | 批 | 座標取校園質心、精度標 `approximate` |
| `R-ANCHOR-MEMBER` (45) | 批 | 同上；質心係已知範圍內最誠實嘅答案 |
| `R-DISTRICT` (5) | **否決整類** | 證據太弱；樣本內有明確錯誤（「隔離病房 → 寶琳」同校園證據矛盾） |

**套用結果**：68 個地點取得真實座標
`fictional` 543 → **475**；`exact` 0 → **8**；`approximate` → 209。

新增 `location_precision: "exact"` —— 原本 schema 冇辦法表達「由 OSM
建築輪廓取到嘅精確座標」，冇呢個值就會被迫降級做 `approximate`，
令前端分唔到「真係知喺邊幢」同「只知喺個區」。

### 11.2 第二遍推斷：先解父、再解子

`R-CONTAIN` 對「D橦大樓醫療室」只能講「依附於『D橦大樓』，但後者亦係
模糊地點」—— 係**阻塞標記**，唔係推斷。

但主 pass 已經解決咗「D橦大樓 = D 座」。所以加第二遍：父項有精確座標
（信心 ≥0.80 且 `osm_way`）就傳播落子項。**一遍做唔到，兩遍就做得到。**

### 11.3 ⚠️ 兩個管線級缺陷（比推斷錯更危險）

**1. 管線非冪等 —— 重跑會失去已套用結果**

`apply` 把 `location_precision` 由 `fictional` 改成 `approximate`／`exact`，
而 `infer` 嘅候選集依賴 `fictional` → 重跑之後嗰批地點跌出候選集，
推斷全部消失（實測 `R-ANCHOR-MEMBER` 45 → 10、`R-EXPLICIT` 全消失）。

呢個極危險，因為重跑係日常操作，而且**唔會報錯**。
→ 候選集加 `inferred_from`；加冪等性回歸測試（推斷→套用→再推斷→
再套用，`locations.geojson` 必須逐字節相同）。

**2. `inference_id` 用序號 —— 審閱決定會套用到錯誤對象**

原本 `inf_0001`、`inf_0002`… 按輸出次序編號。規則改動或去重次序一變，
同一個 id 就指向唔同記錄 —— 實測 `inf_0042` 由「D橦三樓看護室」變成
「大本營（倖存區）」，令已寫入公開資料嘅 `inferred_from` 同審閱決定
**靜默地錯配，審計軌跡完全失效**。

→ id 改為由 subject id 衍生（`inf_loc_0004`），決定檔例外亦改用
`loc_XXXX` 做 key。

### 11.4 推廣至其他實體

地點推斷用嘅三樣嘢同 `entity_kind` 完全無關：正規化分組、可回溯證據鏈、
審閱關卡。所以同一套結構直接套用到角色同事件。

**新增角色推斷（15 條）**：`C-TYPO`（拼寫手誤）／`C-ALIAS-SHARE`（共用
別名）／`C-NORM`（正規化後同名）。

⚠️ **開發過程踩到嚴重假陽性 —— 單用編輯距離會誤判**：

| 配對 | 判讀 |
|---|---|
| 「主角的**母**親」vs「主角的**父**親」 | 完全唔同人 |
| 「公仔之**母**」vs「公仔之**父**」 | 完全唔同人 |
| 「戴**紅**色冷帽竊屍賊」vs「戴**綠**色冷帽竊屍賊」 | 完全唔同人 |

全部只差一個字，但係唔同角色。修正：單字替換要分辨**助詞**同**反義詞**
（30 對）——
- 助詞差異（嘅／的／之）→ 同一人，信心 0.80
- 反義詞替換 → **直接跳過，唔出推斷**
- 其他替換 → 0.50（需人手判斷）
- 冇共同章節 → 上限 0.45

**安全邊界**：地點推斷只改座標（可回復）；角色合併會把兩個實體合成
一個（難回復）。所以 `apply_place_inferences.py` **只處理 location**，
角色一律留待人手。已加測試鎖住呢個邊界。

### 11.5 其餘區域嘅 LOD 圖磚

用**實際地點分佈**揀區域，唔係憑感覺。239 個有真實座標嘅地點按
0.05° 網格分群：

| 叢集 | 數量 | 新增圖磚 |
|---|---|---|
| (114.25, 22.30) | **180** | `tko-campus`（調景嶺 VTC 校園，0.036°） |
| (114.25, 22.35) | **41** | `tko-north`（坑口／寶琳，0.068°） |
| (114.30, 22.30) | 9 | 由 `tko-district` 覆蓋 |
| (114.30, 22.35) | 5 | 由 `tko-region` 覆蓋 |
| (114.15, 22.30) | 3 | 由 `tko-region` 覆蓋 |

現時共 **6 層**（10.22 MiB，前端按需載入，只下載當前層）。

**⚠️ 修正：`MAX_SCALE` 同圖磚跨度唔匹配（新圖磚永遠揀唔到）**

揀層規則係「最窄但仍然完全覆蓋視窗」。視窗最窄寬度 =
`BASE_VIEW.w / MAX_SCALE`。如果圖磚跨度細過呢個值，佢就**永遠**覆蓋
唔到視窗 —— 即係永遠唔會被揀到。

實測：`MAX_SCALE = 12` → 最窄視窗 0.0583°；而 `tko-campus` 跨度只有
0.036°、`tko-north` 只有 0.068°，兩者幾乎永遠用唔到，白白多咗 3.2 MiB
資產。**呢個缺陷唔會報錯、唔會影響其他功能，只係靜默地浪費。**

→ `MAX_SCALE` 12 → 30（最窄視窗 0.0233°）；加回歸測試鎖住不變式。

修正後實測（真實瀏覽器逐級放大到目標）：
`調景嶺校園 → tko-campus`（0.0233°）、`坑口／寶琳 → tko-street`（0.0300°）、
`康城 → tko-district`（0.0300°）。

對照圖：`docs/progress/assets/phase-j-lod-tiers.png`

### 11.6 閘門狀態

| 閘門 | 狀態 |
|---|---|
| `npm run typecheck` / `lint` | ✅ 0 errors |
| `vitest` | ✅ **57/57**（54 → 57） |
| `pytest` | ✅ **163/163**（156 → 163） |
| `validate_public_data.py` | ✅ 全部通過 |
| `audit_release.py --strict` | ✅ RELEASE AUDIT PASSED |
| 推斷 schema 驗證 | ✅ 597/597 通過 |

推送狀態：**已同步**（`2b2b769` → `493e834`）。

### 11.7 仍然未做

1. **507 條 `R-NO-EVIDENCE`** —— 部分其實有足夠上文下理，只係需要新增
   規則（例如以角色活動範圍推斷、以事件章節序列推斷）。
2. **15 條角色合併候選** —— 需人手判斷（有啲形成鏈，例如
   「不破尚~聽不破尚」同「不破尚~陸軒」）。
3. **其他實體類型** —— 路線可信度、時間線一致性尚未做。

---

## 12. 第八輪：新推斷規則、角色合併、路線／時間線一致性

用戶指示：繼續 (1) 未推斷個案、(3) 路線可信度同時間線一致性，
(2) 角色合併由我自行判斷。

### 12.1 (1) 未推斷個案：507 → 444

新增兩條規則，並且修咗一個令大量匹配失敗嘅 bug。

**Bug：OSM 名稱係中英雙語**

OSM 嘅 `name` 通常係「將軍澳中心 Park Central」，而 `name:zh` 才係
純中文。原本只索引 `name`，令中文名精確匹配大量失敗 —— 實測
「將軍澳中心」「將軍澳廣場」「彩明商場」「新都城中心一期」**全部配對
唔到，明明 OSM 有記錄**。

→ 每個設施同時用 `name`／`name:zh`／`name` 嘅中文前綴索引。

**`R-NAME-PLACE`（20 條）**：名稱直接對應 OSM 記錄，唔需要任何推斷。

⚠️ 必須雙重過濾，否則命中大量假陽性：

| 名稱 | 錯誤配對到 |
|---|---|
| 「學校」 | 上水 (114.2226, 22.5461) |
| 「飯堂」 | 粉嶺 (114.1639, 22.5217) |
| 「禮堂」 | 屯門 (113.9621, 22.3748) |
| 「停車場」 | 元朗 (114.0420, 22.3621) |

1. 名稱要有**識別性詞幹**（排除通用詞）
2. 配對要落喺**故事設定區域**內（`STORY_REGION`）

**`R-DESC-PLACE`（32 條）**：描述直接提及真實地區名（例如「…位於寶琳
嘅…」）—— 比 `R-DISTRICT` 嘅同章共現強，因為描述係**直接證據**而非
統計相關。只准真正地區名，排除「商場」「醫院」「公園」呢類通用詞。

**累計成果**：`fictional` 543 → **434**（解決 109 個 = 20%）；
`exact` 0 → **28**；`approximate` → 202。

### 12.2 (2) 角色合併：342 → 331

逐條核對章節範圍、aliases、角色描述：

**合併 11 個**

| 配對 | 依據 |
|---|---|
| 我嘅母親／我母親 → 我的母親 | 同章 76/77，純助詞差異 |
| 攻擊學嘅病者老師 → 攻擊學病者老師 | 同章 68 |
| 貴華嘅母親／貴華嘅父親 → 貴華母親／貴華父親 | 同章 197 |
| 敘述者 → 敘事者 | 敘事者 aliases **本身已經包含**「敘述者」 |
| 德蘭教主 → 德蘭女教主 | 章節相鄰 127/131 vs 130 |
| 聽不破尚 → 不破尚 | 後者只 ch75、無 aliases，係斷詞錯誤 |
| 竊屍賊 → 盜屍賊 | 盜／竊同義 |
| 鳥嘴／敘事者 → 主角 | 三者 aliases 互相指向（『M』同『老師』同時出現喺鳥嘴同敘事者） |

**否決 1 個**：不破尚／陸軒 —— 兩者共用別名「佬大」（老大），但章節
範圍**完全唔重疊**（18–63 vs 154–160），係唔同組織嘅首領。共用「佬大」
只係因為呢個係通用稱呼。

**保留待查 1 個**：王達尼／阿達尼 —— 相隔 74 章，需要人手核對章節內容。

`scripts/merge_characters.py` 嘅設計要點：
- **傳遞閉包**：主角~鳥嘴~敘事者 要摺到不動點，唔可以只摺一層
- **引用完整性**：更新 events／routes／timeline 嘅角色引用
- **可追溯**：被合併嘅名保留做 canonical 嘅 `aliases`
- **逐條理由**記錄喺 `character-merge-decisions.json`

### 12.3 (3) 路線可信度

**⚠️ 修正：路線幾何冇跟隨地點更新**

第一次套用推斷時只傳播到 `events.geojson`，**漏咗 `routes.geojson`**
（當時寫 `key=None` 直接跳過）。結果路線幾何仍然係舊嘅虛構座標，
而地點已經移到真實位置 —— 出現「真-真」線段長 **37.5 km** 嘅荒謬結果
（「皇宮」同「新都城商場」明明喺同一個座標，路線卻畫成相距 37 km）。
前端會照樣繪製呢條假線。

**呢個缺陷唔會令驗證器報錯**（驗證器只檢查 events 同 location 一致），
所以極易漏。

→ 傳播改為用**全部地點**對賬（唔止今次改動嘅）。用「今次改動」嘅話，
第一次漏咗之後 `moved` 已經係空，routes 永遠唔會修正。

| 指標 | 修正前 | 修正後 |
|---|---|---|
| 真-真段中位數 | 1,886 m | **876 m** |
| 真-真段最大 | 37,527 m | **4,457 m** |
| 真-真 >5 km 段數 | 39 | **0** |
| 真-真段總數 | 213 | 213（39 → 213 係推斷帶來嘅改善） |

### 12.4 (3) 時間線一致性

**⚠️ 時間線冇排序**

`date_label` 寫住「按章節先後」，但實測有 **41 個逆序對**
（index 15 係 ch3、index 16 係 ch1）。因為 builder 按 event id 次序產生，
而 event id 唔一定跟章節遞增。前端照陣列次序顯示 → **用戶會見到時間
倒流**。

→ 新增 `scripts/normalize_public_data.py`（確定性、可重複執行）：
排序之後 **41 → 0**，1,130 條記錄位置改變。

順帶重算路線精度：全部 42 條原本都係 `fictional`，但推斷之後部分路線
大部分 waypoint 已係真實 → 13 條 `approximate`、29 條 `fictional`。

⚠️ 只可以用 `route.schema.json` 允許嘅值（`reference`／`approximate`／
`fictional`），**唔可以自己發明 `district`** —— 呢個係 schema 契約。

### 12.5 新增測試

`tests/test_data_normalization.py`（13 項）：時間線排序、路線座標同
waypoint 一致、真-真線段 ≤8 km、精度值合法、角色合併完整性、
兩支腳本嘅冪等性。

### 12.6 閘門狀態

| 閘門 | 狀態 |
|---|---|
| `npm run typecheck` / `lint` | ✅ 0 errors |
| `vitest` | ✅ 57/57 |
| `pytest` | ✅ **176/176**（163 → 176） |
| `validate_public_data.py` | ✅ 全部通過 |
| `audit_release.py --strict` | ✅ RELEASE AUDIT PASSED |

推送狀態：**已同步**（`72271fe` → `a01167f`）。

### 12.7 仍然未做

1. **444 條 `R-NO-EVIDENCE`** —— 需要更強嘅規則（例如角色活動範圍、
   事件章節序列、描述語意相似度）。
2. **王達尼／阿達尼** —— 需人手核對章節內容。
3. **`events.characters` 陣列從未填充** —— 1,796 條事件全部係空陣列，
   即係事件同角色之間冇連結。呢個係一個獨立嘅資料缺口。
